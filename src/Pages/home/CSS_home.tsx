import { makeStyles } from '@mui/styles';

export const useStylesHome=makeStyles({
    body: {
        backgroundColor: 'lightblue',
        position: 'absolute',
        top: 0,
        left: 0,
        width: '100%',
        height: '100%',
      }
});